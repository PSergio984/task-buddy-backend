# PLAN 02-01: Security Middleware and Password Hashing Upgrade

## Overview
Hardening the backend by upgrading password hashing to Argon2 and enforcing strict security headers and CORS policies.

## Proposed Changes

### 1. Dependencies
- Update `requirements.txt` to include `argon2-cffi`.

### 2. Password Hashing (`app/security.py`)
- Ensure `pwd_context` is configured for `argon2`.
- Add `needs_rehash` logic if necessary (though we'll focus on new passwords for now).

### 3. Middleware & Headers (`app/main.py`)
- Update `add_security_headers` middleware.
- Ensure CORS `allow_origins` is strictly controlled.

## Tasks

- [ ] Update `requirements.txt` with `argon2-cffi`
- [ ] Verify `app/security.py` hashing scheme
- [ ] Harden `add_security_headers` in `app/main.py`
- [ ] Add security test cases in `tests/test_security_headers.py`
- [ ] Run all tests and verify pass rate

## Verification
- `pytest tests/test_security_headers.py`
- `curl -I http://localhost:8000/api/v1/tasks` (manual)
