# Phase 3.5: Premium UI, Projects, and Data Seeding - Execution Plan

## Context
This phase will upgrade the user interface to a premium aesthetic, implement Project (workspace) management on the frontend, fix sidebar bugs, and introduce a backend script for seeding the database with dummy data.

## Tasks

### 1. Backend Data Seeding Script
- [ ] Create a `scripts/seed.py` file in the backend repository.
- [ ] Implement logic to connect to the database (SQLite/PostgreSQL) using SQLAlchemy.
- [ ] Generate dummy data:
  - 1-2 Users
  - 2-3 Projects (Groups) for each user
  - 5-10 Tasks per project, some with Subtasks and Tags
- [ ] Add instructions in the README on how to run the seed script.

### 2. Frontend Sidebar Fixes
- [ ] Investigate the sidebar component (likely `Sidebar.tsx` or similar).
- [ ] Fix the collapse icon bug so it toggles state properly.
- [ ] Ensure the collapse animation is smooth and feels premium (using Framer Motion if applicable).

### 3. Frontend Projects (Workspaces) Functionality
- [ ] Implement UI for users to create a new Project (utilizing the existing `group.py` backend API).
- [ ] Implement UI for users to view Projects in the sidebar or dashboard.
- [ ] Update the task creation flow to allow assigning a task to a specific Project.
- [ ] Do NOT add label filters to the sidebar. Keep navigation clean.

### 4. Create Task UI Overhaul & Calendar Integration
- [ ] Refactor the "Create Task" modal/page.
- [ ] Remove existing grey colors and replace with a high-contrast, premium aesthetic (utilizing glassmorphism, fluid typography, and proper spacing per the `premium-frontend-ui` skill).
- [ ] Install `react-day-picker` and `date-fns` in the frontend repository.
- [ ] Implement a premium date picker component for selecting task due dates.
- [ ] Ensure all form fields have proper hover, focus, and error states.

### 5. Review & Polish (Premium UI Standards)
- [ ] Review all changes against the `premium-frontend-ui` guidelines.
- [ ] Verify scroll animations (if applicable) and layout transitions are perfectly smooth (hardware accelerated).
- [ ] Ensure the overall experience feels cohesive, modern, and high-end.

## Completion Criteria
- Dummy data can be easily seeded using a single Python command.
- Sidebar collapses and expands flawlessly with a premium animation.
- Projects can be created and tasks can be assigned to them.
- "Create Task" form looks visually stunning and uses `react-day-picker` for dates.
- No regression in existing task CRUD functionality.
