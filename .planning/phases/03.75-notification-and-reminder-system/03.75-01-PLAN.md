---
phase: 03.75-notification-and-reminder-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [requirements.txt, app/models/notification.py, app/crud/notification.py]
autonomous: true
requirements: [NOTI-01, NOTI-02, NOTI-03]

must_haves:
  truths:
    - "Notification and PushSubscription models exist in the database schema."
    - "CRUD operations for notifications and subscriptions are implemented and follow the project's transaction pattern."
  artifacts:
    - path: "app/models/notification.py"
      provides: "ORM models for Notifications and PushSubscriptions"
    - path: "app/crud/notification.py"
      provides: "CRUD logic for managing notification state"
  key_links:
    - from: "app/models/notification.py"
      to: "app/models/user.py"
      via: "ForeignKey(user_id)"
---

<objective>
Setup the core database infrastructure and CRUD layer for the Notification and Reminder system. This includes defining the data models, generating migrations, and implementing the persistence logic.

Purpose: Provide a durable storage layer for multi-channel notifications and browser push subscriptions.
Output: ORM models, Alembic migration, and CRUD functions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.75-notifications-context.md
@.planning/phases/03.75-notification-and-reminder-system/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies and create notification models</name>
  <files>requirements.txt, app/models/notification.py</files>
  <action>
    1. Add `pywebpush==1.14.0` to `requirements.txt`.
    2. Create `app/models/notification.py`.
    3. Implement `NotificationType` Enum: `REMINDER_BEFORE`, `REMINDER_DUE`, `REMINDER_OVERDUE`, `SYSTEM`.
    4. Implement `Notification` model with: `id`, `user_id` (FK), `task_id` (FK, optional), `type` (Enum), `title`, `message`, `is_read` (bool, default False), `action_url` (optional), `created_at` (timestamp).
    5. Implement `PushSubscription` model with: `id`, `user_id` (FK), `endpoint` (Unique), `p256dh`, `auth`, `created_at`.
    6. Ensure models use SQLAlchemy 2.0 style type annotations (Mapped/mapped_column) per REFI-03.
    7. Update `app/models/__init__.py` to export the new models.
  </action>
  <verify>
    <automated>python -c "from app.models.notification import Notification, PushSubscription; print('Success')"</automated>
  </verify>
  <done>Models defined and exported, dependencies updated.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and apply Alembic migration</name>
  <files>alembic/versions/*.py</files>
  <action>
    1. Generate a new migration: `alembic revision --autogenerate -m "add_notifications_and_subscriptions"`.
    2. Review the generated script to ensure it captures both tables and the Enum type.
    3. Apply the migration: `alembic upgrade head`.
  </action>
  <verify>
    <automated>alembic history | head -n 1; # Should show the new migration</automated>
  </verify>
  <done>Database schema updated with new tables.</done>
</task>

<task type="auto">
  <name>Task 3: Implement notification CRUD layer</name>
  <files>app/crud/notification.py</files>
  <action>
    Implement CRUD functions in `app/crud/notification.py` following the project's decoupled transaction pattern (no `db.commit()` or `db.refresh()` per REFI-01):
    1. `get_notifications(db, user_id, skip=0, limit=100, is_read=None)`: List user notifications with pagination and filtering.
    2. `create_notification(db, notification_in)`: Create a new notification.
    3. `mark_notification_as_read(db, notification_id, user_id)`: Mark a specific notification as read.
    4. `create_or_update_push_subscription(db, user_id, subscription_in)`: UPSERT logic for push subscriptions (match on endpoint).
    5. `delete_push_subscription(db, endpoint)`: Remove an invalid subscription (needed for 410 Gone handling later).
  </action>
  <verify>
    <automated>pytest tests/test_notifications_crud.py (Note: Wave 0 task might be needed if tests don't exist, but we'll include basic verification in Wave 4)</automated>
  </verify>
  <done>CRUD functions implemented and aligned with architectural standards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database | Persists sensitive push subscription tokens (p256dh, auth). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.75-01 | Information Disclosure | PushSubscription | mitigate | Ensure push subscription secrets are only accessible to the owner and the backend worker. |
</threat_model>

<verification>
1. `pip install -r requirements.txt` passes.
2. `alembic upgrade head` completes without error.
3. CRUD functions can be imported and instantiated.
</verification>

<success_criteria>
- Notification and PushSubscription tables exist in DB.
- CRUD layer supports listing, creating, and updating notification state.
</success_criteria>

<output>
After completion, create `.planning/phases/03.75-notification-and-reminder-system/03.75-01-SUMMARY.md`
</output>
