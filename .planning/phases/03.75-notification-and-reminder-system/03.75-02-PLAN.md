---
phase: 03.75-notification-and-reminder-system
plan: 02
type: execute
wave: 2
depends_on: [03.75-01]
files_modified: [app/schemas/notification.py, app/api/routers/notifications.py, app/main.py]
autonomous: true
requirements: [NOTI-02, NOTI-03]

must_haves:
  truths:
    - "Endpoints for listing notifications and marking as read are available."
    - "Endpoint for registering push subscriptions is functional."
  artifacts:
    - path: "app/schemas/notification.py"
      provides: "Pydantic validation for notification and subscription data"
    - path: "app/api/routers/notifications.py"
      provides: "REST endpoints for notification management"
  key_links:
    - from: "app/main.py"
      to: "app/api/routers/notifications.py"
      via: "app.include_router()"
---

<objective>
Expose the notification and push subscription functionality through REST API endpoints. This enables the frontend to fetch notifications, mark them as read, and register the browser's push tokens.

Purpose: Bridge the backend persistence layer with the user interface.
Output: Pydantic schemas and FastAPI router.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03.75-notifications-context.md
@.planning/phases/03.75-notification-and-reminder-system/RESEARCH.md
@.planning/phases/03.75-notification-and-reminder-system/03.75-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create notification schemas</name>
  <files>app/schemas/notification.py</files>
  <action>
    Create Pydantic schemas in `app/schemas/notification.py`:
    1. `NotificationRead`: Response schema for notifications (including `task_id`).
    2. `PushSubscriptionCreate`: Input schema for browser subscriptions (endpoint, p256dh, auth).
    3. `PushSubscriptionRead`: Response schema for subscriptions.
    4. Ensure schemas use Pydantic v2 `ConfigDict(from_attributes=True)`.
  </action>
  <verify>
    <automated>python -c "from app.schemas.notification import NotificationRead; print('Success')"</automated>
  </verify>
  <done>Schemas defined for API request/response validation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement notification router and register in main</name>
  <files>app/api/routers/notifications.py, app/main.py</files>
  <action>
    1. Create `app/api/routers/notifications.py` with the following endpoints:
       - `GET /`: List notifications for current user (paginated).
       - `PATCH /{id}/read`: Mark notification as read (verify ownership).
       - `POST /push-subscription`: Register/update push subscription for current user.
    2. Use `get_current_active_user` dependency for all endpoints.
    3. Integrate with CRUD functions from `app.crud.notification`.
    4. Register the new router in `app/main.py` with prefix `/api/v1/notifications` and tags `["notifications"]`.
  </action>
  <verify>
    <automated>pytest tests/routers/test_notifications.py (Basic import/registration check)</automated>
  </verify>
  <done>REST API endpoints functional and registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted JSON data for push subscriptions and notification IDs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.75-02 | Tampering | `PATCH /{id}/read` | mitigate | Verify `notification.user_id == current_user.id` before allowing state change. |
| T-03.75-03 | Spoofing | `POST /push-subscription` | mitigate | Ensure subscription is always linked to `current_user.id`. |
</threat_model>

<verification>
1. FastAPI Swagger UI (`/docs`) shows the new `/notifications` endpoints.
2. Endpoint calls return 401 Unauthorized when no token is provided.
</verification>

<success_criteria>
- Frontend can register push subscriptions.
- Frontend can list and mark notifications as read.
</success_criteria>

<output>
After completion, create `.planning/phases/03.75-notification-and-reminder-system/03.75-02-SUMMARY.md`
</output>
