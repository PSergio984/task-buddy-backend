---
phase: 03.75-notification-and-reminder-system
plan: 03
type: execute
wave: 3
depends_on: [03.75-02]
files_modified: [app/config.py, .env.example, app/celery_app.py, app/tasks.py]
autonomous: true
requirements: [NOTI-01, NOTI-03]
user_setup:
  - service: WebPush
    why: "VAPID keys are required for browser push notifications"
    env_vars:
      - name: VAPID_PUBLIC_KEY
        source: "Generated via pywebpush (see RESEARCH.md)"
      - name: VAPID_PRIVATE_KEY
        source: "Generated via pywebpush (see RESEARCH.md)"

must_haves:
  truths:
    - "Celery Beat is configured to run the reminder scanner every 10 minutes."
    - "System correctly identifies tasks needing reminders and dispatches them via Email and Push."
  artifacts:
    - path: "app/tasks.py"
      provides: "process_reminders and send_push_notification tasks"
  key_links:
    - from: "app/celery_app.py"
      to: "app/tasks.py"
      via: "celery_app.conf.beat_schedule"
---

<objective>
Implement the core logic for the automated reminder system and multi-channel notification dispatch. This includes configuring the periodic scheduler (Celery Beat) and implementing the background tasks that scan for upcoming/overdue tasks and send notifications via WebPush and Email.

Purpose: Automate user engagement and task follow-ups.
Output: Celery Beat configuration and specialized worker tasks.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03.75-notifications-context.md
@.planning/phases/03.75-notification-and-reminder-system/RESEARCH.md
@.planning/phases/03.75-notification-and-reminder-system/03.75-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add VAPID config and configure Celery Beat</name>
  <files>app/config.py, .env.example, app/celery_app.py</files>
  <action>
    1. Update `app/config.py` to include `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, and `VAPID_ADMIN_EMAIL`.
    2. Add these variables to `.env.example`.
    3. Update `app/celery_app.py` to include the `beat_schedule`:
       - `process-reminders-every-10-mins`: calls `app.tasks.process_reminders` every 10 minutes (600 seconds).
  </action>
  <verify>
    <automated>python -c "from app.config import config; print(config.VAPID_PUBLIC_KEY)"</automated>
  </verify>
  <done>Configuration and scheduler updated.</done>
</task>

<task type="auto">
  <name>Task 2: Implement process_reminders logic</name>
  <files>app/tasks.py</files>
  <action>
    Implement `process_reminders` task in `app/tasks.py`:
    1. Use `AsyncSessionLocal` to query incomplete tasks.
    2. Logic for three windows (Before, At, Overdue) as defined in CONTEXT.md.
    3. Deduplication logic: Check `tbl_notifications` for existing records of the same `user_id`, `task_id`, `type`, and `time_window` before sending.
    4. For each identified task, create a `Notification` record (In-App) and enqueue `send_push_notification` and `send_confirmation_email` (reusing existing email logic).
  </action>
  <verify>
    <automated>pytest tests/test_tasks.py (Check if task is discoverable)</automated>
  </verify>
  <done>Reminder scanning logic implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement send_push_notification logic</name>
  <files>app/tasks.py</files>
  <action>
    Implement `send_push_notification` task in `app/tasks.py`:
    1. Fetch user's `PushSubscription` records.
    2. Use `pywebpush.webpush` to encrypt and send data to browser push services.
    3. Catch `WebPushException`: if status is 410 (Gone), delete the subscription record from DB.
    4. Handle timeouts and retries according to standard worker patterns.
  </action>
  <verify>
    <automated>python -c "from pywebpush import webpush; print('Success')"</automated>
  </verify>
  <done>Browser push delivery logic implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker -> External Push Service | Sends encrypted data via HTTPS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.75-04 | Deny of Service | Celery Worker | mitigate | Use rate limiting for push notification tasks to prevent overloading the external push service or local worker pool. |
</threat_model>

<verification>
1. Run `celery -A app.celery_app worker -B -l info` to verify Beat and Worker start correctly.
2. Logs show `process_reminders` task is scheduled and executed.
</verification>

<success_criteria>
- Reminders are automatically triggered based on task due dates.
- Notifications are persisted in DB and dispatched to Email/Push.
</success_criteria>

<output>
After completion, create `.planning/phases/03.75-notification-and-reminder-system/03.75-03-SUMMARY.md`
</output>
