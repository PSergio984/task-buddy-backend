---
phase: 03.75-notification-and-reminder-system
plan: 04
type: execute
wave: 4
depends_on: [03.75-03]
files_modified: [tests/test_notifications.py, tests/routers/test_notifications.py]
autonomous: true
requirements: [NOTI-01, NOTI-02, NOTI-03]

must_haves:
  truths:
    - "Notification system behavior is verified with automated tests."
    - "Edge cases like expired push subscriptions and duplicate reminders are handled."
  artifacts:
    - path: "tests/test_notifications.py"
      provides: "Unit and logic tests for notifications"
    - path: "tests/routers/test_notifications.py"
      provides: "Integration tests for notification endpoints"
---

<objective>
Verify the end-to-end functionality of the Notification and Reminder system. This plan focuses on writing robust tests to cover models, CRUD logic, API endpoints, and background task behaviors, ensuring reliability and preventing regressions.

Purpose: Guarantee system quality and resilience.
Output: Comprehensive test suite for the notification subsystem.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03.75-notifications-context.md
@.planning/phases/03.75-notification-and-reminder-system/RESEARCH.md
@.planning/phases/03.75-notification-and-reminder-system/03.75-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement unit and integration tests for Notifications</name>
  <files>tests/test_notifications.py, tests/routers/test_notifications.py</files>
  <action>
    1. Create `tests/test_notifications.py`:
       - Test CRUD operations (create, list, upsert subscription).
       - Test `process_reminders` logic with mocked time and task data.
       - Verify deduplication prevents multiple notifications for the same event.
    2. Create `tests/routers/test_notifications.py`:
       - Test `GET /api/v1/notifications/` with different filters.
       - Test `PATCH /api/v1/notifications/{id}/read` (including ownership check).
       - Test `POST /api/v1/notifications/push-subscription`.
  </action>
  <verify>
    <automated>pytest tests/test_notifications.py tests/routers/test_notifications.py</automated>
  </verify>
  <done>Core notification logic and API verified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement mock-based tests for Celery tasks</name>
  <files>tests/test_notification_tasks.py</files>
  <action>
    Create `tests/test_notification_tasks.py`:
    1. Mock `pywebpush.webpush` to verify `send_push_notification` calls it with correct parameters.
    2. Test the "410 Gone" scenario: Ensure subscription is deleted when mock raises `WebPushException` with status 410.
    3. Mock email dispatch to ensure `process_reminders` triggers both Email and Push when appropriate.
  </action>
  <verify>
    <automated>pytest tests/test_notification_tasks.py</automated>
  </verify>
  <done>Background tasks and external integrations verified via mocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Code -> App Code | Tests execute internal logic and API endpoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.75-05 | Information Disclosure | Test Logs | mitigate | Ensure sensitive data like VAPID keys or push tokens are not printed in test logs. |
</threat_model>

<verification>
1. `pytest tests/test_notifications.py tests/routers/test_notifications.py tests/test_notification_tasks.py` all pass.
2. Code coverage for the new files is > 90%.
</verification>

<success_criteria>
- All notification-related tests pass.
- System is verified against requirements (Email, In-App, Push).
</success_criteria>

<output>
After completion, create `.planning/phases/03.75-notification-and-reminder-system/03.75-04-SUMMARY.md`
</output>
